type: object
properties:
  id:
    type: integer
    format: int64
  submitDate:
    type: string
    format: date-time
  assignmentDate:
    type: string
    format: date-time
  status:
    type: string
    enum: [ REQUESTED, GRANTED, REJECTED ]
  content:
    type: string
  enrollmentId:
    type: integer
    format: int64

