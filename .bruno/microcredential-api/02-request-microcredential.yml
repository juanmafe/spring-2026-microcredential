info:
  name: request microcredential
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/microcredentials"
  body:
    type: json
    data: |2-
          {
            "submitDate": "2026-05-16",
            "content": "Completed all course requirements with excellence",
            "enrollmentId": 1
          }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

