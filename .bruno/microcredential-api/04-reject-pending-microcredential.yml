info:
  name: reject pending microcredential
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{baseUrl}}/microcredentials/1/rejected"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
