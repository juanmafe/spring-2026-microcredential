info:
  name: request course microcredentials
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/microcredentials/course/4"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

