info:
  name: approve pending microcredential
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{baseUrl}}/microcredentials/1/status"
  body:
    type: json
    data: |2-
          {
            "status": "GRANTED"
          }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

