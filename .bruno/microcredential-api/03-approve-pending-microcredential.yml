info:
  name: approve pending microcredential
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{baseUrl}}/microcredentials/1/approve"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
